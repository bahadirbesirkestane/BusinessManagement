@model MaterialRequestTemplateFormViewModel
@{
    ViewData["Title"] = "İhtiyaç Şablonunu Düzenle";
}

<section class="panel-block form-panel">
    <div class="panel-heading">
        <div>
            <p class="section-kicker">Malzeme ihtiyaçları</p>
            <h2>İhtiyaç şablonunu düzenle</h2>
        </div>
    </div>

    <form asp-action="Edit" method="post" class="app-form">
        <input asp-for="Id" type="hidden" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <partial name="_TemplateForm" model="Model" />
        <div class="form-actions">
            <a class="btn btn-outline-secondary" asp-action="Details" asp-route-id="@Model.Id">Vazgeç</a>
            <button class="btn btn-primary" type="submit">Kaydet</button>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
